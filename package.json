{
  "name": "polymarket-screener",
  "version": "0.1.0",
  "private": true,
  "description": "Alerts-only Polymarket wallet screening worker.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.33.0"
  }
}
